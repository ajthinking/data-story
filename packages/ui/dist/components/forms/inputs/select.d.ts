import { UseFormRegister } from 'react-hook-form';
export declare const Select: ({ register, label, id, options }: {
    register: UseFormRegister<Record<string, any>>;
    label: string;
    id: string;
    options: string[];
}) => JSX.Element;
