import { UseFormRegister } from 'react-hook-form';
export declare const Text: ({ register, label, id }: {
    register: UseFormRegister<Record<string, any>>;
    label: string;
    id: string;
}) => JSX.Element;
