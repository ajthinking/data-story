# JSX bug 🐛
Whenever using `DataStory` component in docs, we get:

```
'DataStory' cannot be used as a JSX component.
  Its type '({ server, diagram, callback, hideToolbar, }: { server?: ServerConfig; diagram?: Diagram; hideToolbar?: boolean; callback?: (options: any) => void; }) => Element' is not a valid JSX element type.ts(2786)
```