{
  "name": "data-story",
  "private": true,
  "workspaces": [
    "packages/core",
    "packages/ui",
    "packages/client",
    "packages/docs"
  ],
  "scripts": {
    "dev": "yarn kill && turbo run dev --parallel",
    "kill": "kill-port 3000 3001 3100",
    "build": "turbo build"
  },
  "version": "0.0.1",
  "packageManager": "yarn@3.6.1",
  "devDependencies": {
    "kill-port": "^2.0.1",
    "turbo": "^1.10.12"
  },
  "dependencies": {
    "reactflow": "^11.8.1"
  }
}
