//import { DiagramModelBuilder } from '../../../src/core/DiagramModelBuilder'

it.skip('can build diagrams programatically', () => {

	//let diagram = DiagramModelBuilder.begin().finish()

	//diagram.serialize()
});